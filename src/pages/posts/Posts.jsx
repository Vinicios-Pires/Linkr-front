import styled from "styled-components";
import { Post } from "./style";

export default function Post (){
    const { id, message, image, username, postData, index } = props;
    const { postDescription, postUrl, postImage, postTitle } = postData;



    return(
        <Post>
          <> </>  
        </Post>
    )
}